name: Deploy Development Preview

on:
  push:
    branches: [development]

permissions:
  contents: read

concurrency:
  group: netlify-development
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: netlify-development
      url: https://usms-mockup-dev.netlify.app
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - run: npm install
      - name: Write keys.json
        run: echo '{"googleMaps":"${{ secrets.GOOGLE_MAPS_API_KEY }}"}' > src/_data/keys.json
      - run: npm run build
      - name: Deploy to Netlify (development branch preview)
        run: |
          npm install -g netlify-cli
          netlify deploy --dir=_site --prod --message="development @ ${{ github.sha }}" --site=ccd2bd96-1a67-4d15-b94a-8599aff0127a --json
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

  # Plan Item 4 — a separate job, not a step in `deploy` above, so review
  # latency/failure never blocks or delays the actual Netlify deploy. Reads
  # the diff and posts a review to Slack; never blocks, never writes to the
  # repo (permissions: contents: read, same as `deploy`), never merges or
  # comments on anything.
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # required to mint the GitHub OIDC token WIF exchanges for Anthropic auth
    env:
      ANTHROPIC_FEDERATION_RULE_ID: ${{ vars.ANTHROPIC_FEDERATION_RULE_ID }}
      ANTHROPIC_ORGANIZATION_ID: ${{ vars.ANTHROPIC_ORGANIZATION_ID }}
      ANTHROPIC_SERVICE_ACCOUNT_ID: ${{ vars.ANTHROPIC_SERVICE_ACCOUNT_ID }}
      ANTHROPIC_WORKSPACE_ID: ${{ vars.ANTHROPIC_WORKSPACE_ID }}
      ANTHROPIC_IDENTITY_TOKEN_FILE: /tmp/anthropic-oidc-token
      GITHUB_EVENT_BEFORE: ${{ github.event.before }}
      GITHUB_SHA: ${{ github.sha }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0 # needs real history to diff against, not a shallow clone
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - name: Fetch GitHub OIDC token for Anthropic WIF
        run: |
          curl -sS -H "Authorization: Bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=https://api.anthropic.com" \
            | jq -r .value > "$ANTHROPIC_IDENTITY_TOKEN_FILE"
      - name: Install claude CLI
        run: npm install -g @anthropic-ai/claude-code@latest
      - name: Run code review
        id: review
        run: node scripts/run-code-review.js
      - name: Post review to Slack
        if: steps.review.outputs.review != ''
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          # Passed via env, not interpolated into the script body — the review
          # text is LLM-generated and could contain backticks/$()/quotes that
          # would otherwise be interpreted as shell syntax (GitHub's own
          # documented script-injection risk for ${{ }} inside `run:`).
          REVIEW_TEXT: ${{ steps.review.outputs.review }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "::warning::SLACK_WEBHOOK_URL secret not set — skipping Slack notification."
            exit 0
          fi
          TEXT="Code review for push to development ($COMMIT_SHA):
          $REVIEW_TEXT"
          curl -sf -X POST -H 'Content-type: application/json' \
            --data "$(jq -n --arg text "$TEXT" '{text: $text}')" \
            "$SLACK_WEBHOOK_URL"
